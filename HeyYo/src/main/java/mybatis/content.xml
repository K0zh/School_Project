<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.heyyo.content.model.Content">
	<select id="GetAllContent" resultType="HashMap">
		select * from contents
	</select>
	
	<select id="GetContentById" resultType="MyContent" parameterType="int">
		select * from contents where id = #{id}
	</select>
	
	<insert id="InsertTalentData" parameterType="MyContent">
		insert into contents(id, num, subject, talent, s_day, address, location, personnel, gender, content, closed, type)
		values(#{id}, conseq.nextval, #{subject}, #{talent}, #{s_day}, #{address}, #{location}, #{personnel}, #{gender}, #{content}, 'open', #{type})
	</insert>
	
</mapper>